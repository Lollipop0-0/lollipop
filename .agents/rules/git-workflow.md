# Git Workflow & Behavioral Rules

- **Do NOT push automatically**: NEVER run `git push` unless the user explicitly requests to push. Keep all commits and changes local.
- **No screenshots or generated pictures**: Do not generate images or take screenshots unless explicitly requested by the user.
